import Navbar from '@/components/Navbar';
import React from 'react';

const Projects = () => {
    return (
        <div>
            <Navbar/>
            project
        </div>
    );
};

export default Projects;